PORT=4000
DB_PATH=./trackwise.db
SESSION_SECRET=change-this-to-a-long-random-string
# Where the React dev server runs, so we can allow it to send cookies cross-origin
CLIENT_ORIGIN=http://localhost:5173
